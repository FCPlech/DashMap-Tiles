# Release staging + big data live as GitHub Release assets, never in git.
dist/
# Local build workspace (extracts, tile trees, per-region work dirs) - same rule.
build/
*.zip
*.zip.part-*
*.tar
*.tar.part-*
*.sqlite
*.osm.pbf
*.osm.bz2
valhalla_tiles/
custom_files/
__pycache__/
